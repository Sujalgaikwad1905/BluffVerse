Added the core play-card engine for BluffVerse. Introduced a dedicated validation layer to enforce game rules before state mutations, implemented card removal, pile updates, claimed-rank tracking, bluff window activation, pass-cycle reset, and immediate win detection. The game flow now follows a clean architecture: Socket → Game Engine → Event Queue → Validator → Manager → Game State.